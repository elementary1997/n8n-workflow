# Daily Tasks Workflow

Автоматический workflow для создания ежедневных задач в Google Tasks на основе расписания из Google Sheets. Запускается каждый день в 7:00 и отправляет уведомление в Telegram.

## 🎯 Что делает

- **Каждый день в 7:00** триггер Schedule Trigger запускает workflow
- **Загружает расписание** из Google Sheets по дню недели
- **Создаёт список задач** в Google Tasks с названием дня и даты
- **Добавляет задачи** в Google Tasks с временем и эмодзи
- **Отправляет уведомление** в Telegram о добавлении задач

## 🚀 Быстрый старт

### 1. Подготовка данных

Создай Google Sheets таблицу со следующей структурой:

| День недели |        Задача      | Время | Эмодзи |
|-------------|--------------------|-------|--------|
| Понедельник | Встреча с командой | 09:00 |   📋   |
| Понедельник | Code review        | 14:00 |   👀   |
| Вторник     | Планирование       | 10:00 |   📅   |

### 2. Замена плейсхолдеров

Замени все `{{PLACEHOLDER}}` на свои значения:

```json
{{GOOGLE_TASKS_CREDENTIAL_ID}}      // ID credentials Google Tasks в n8n
{{GOOGLE_SHEETS_CREDENTIAL_ID}}     // ID credentials Google Sheets в n8n
{{TELEGRAM_CREDENTIAL_ID}}          // ID credentials Telegram Bot в n8n
{{GOOGLE_SHEETS_DOC_ID}}            // ID документа (из URL между /d/ и /edit)
{{GOOGLE_SHEETS_SHEET_ID}}          // ID листа (gid= в URL)
{{TELEGRAM_CHAT_ID}}                // ID чата для отправки уведомлений
{{N8N_INSTANCE_ID}}                 // ID вашей n8n инстанции
```

### 3. Импорт в n8n

1. Открой n8n → Create → Import workflow
2. Вставь JSON файл
3. Убедись, что все credentials подключены
4. Активируй workflow

## 📊 Структура workflow

```
Schedule Trigger (7:00)
    ↓
Edit Fields (форматирование даты)
    ↓
HTTP Request (создание списка задач)
    ↓
Edit Fields1 (извлечение ID списка)
    ↓
Load Schedule from Sheets (загрузка по дню недели)
    ↓
Code in JavaScript (инверсия порядка)
    ↓
Loop Over Items (обход каждой задачи)
    ├─→ Create a task (добавление в Google Tasks)
    └─→ Aggregate (сбор результатов)
         ↓
      Send Telegram (уведомление)
```

## ⚙️ Требования

- **n8n** (self-hosted или cloud)
- **Google Account** с доступом к Google Tasks и Google Sheets
- **Telegram Bot** (создан через @BotFather)
- **Credentials в n8n**:
  - Google Tasks OAuth2
  - Google Sheets OAuth2
  - Telegram Bot API

## 🔧 Настройка

### Изменение времени запуска

В ноде **Schedule Trigger** измени cron выражение:

```
0 7 * * *   // Текущее (7:00 каждый день)
0 8 * * *   // 8:00
0 9 * * 1-5 // 9:00 только будни
```

### Изменение текста уведомления

В ноде **Send a text message** измени текст:

```javascript
{{ $('Edit Fields').item.json["Day of month"] }} 
{{ $('Edit Fields').item.json.Month }} 
{{ $('Edit Fields').item.json.Year }} 
- 🏃 Задачи на день добавлены 🏃
```

## 🐛 Troubleshooting

| Проблема                   |                                           Решение                                     | 
|----------------------------|---------------------------------------------------------------------------------------|
| Workflow не запускается    | Проверь, что Schedule Trigger активен и время сервера совпадает с часовым поясом      |
| Ошибка при создании задач  | Убедись, что GOOGLE_TASKS_CREDENTIAL_ID и GOOGLE_SHEETS_DOC_ID заменены корректно     |
| Нет уведомления в Telegram | Проверь TELEGRAM_CHAT_ID и TELEGRAM_CREDENTIAL_ID, убедись что бот в чате админ       |
| Задачи не загружаются      | Проверь, что в Google Sheets правильно указан день недели (название должно совпадать) |

## 📝 Примечание

Workflow использует переменные окружения для всех чувствительных данных.
